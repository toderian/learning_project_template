# Screenshots

Temporary screenshots. Move useful curated images to
`04_areas/<area>/assets/images/<topic-path>/` with timestamp-prefixed
filenames.

Screenshots can expose credentials, personal data, account pages, client data,
or private context. Keep sensitive originals in `.no-commit/` unless they are
redacted and intentionally safe to commit.
