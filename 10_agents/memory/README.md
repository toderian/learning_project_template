# Memory

Agent memory staging area. Promotion to active memory requires approval.

`10_agents/memory/candidates/` is the approved write exception inside protected `10_agents/` for bounded staged memory candidates. Other edits under `10_agents/` require explicit approval.
