# 07_memory

Human recall practice, spaced repetition, retrieval practice, and review logs.

Run due review listings with:

```bash
12_tools/scripts/list_due_memory.py . --date YYYY-MM-DD
```

Notes tagged `example` are skipped by default so scaffold decks do not pollute a new project's due queue. Add `--include-examples` when validating or inspecting the public examples.
